<UserControl x:Class="supervisorioMMS.Views.PrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:supervisorioMMS.Views"
             xmlns:models="clr-namespace:supervisorioMMS.Models"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Caixa de Ferramentas -->
        <Border Grid.Column="0" Background="#FF2D2D2D">
            <StackPanel>
                <TextBlock Text="Componentes" Foreground="White" FontSize="16" FontWeight="Bold" Padding="10"/>
                <ListBox x:Name="ToolboxList" Background="Transparent" BorderThickness="0" Foreground="White">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Padding="10,5"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="Toolbox_PreviewMouseLeftButtonDown"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Área de Edição (Canvas) -->
        <ItemsControl Grid.Column="1" x:Name="EditorItemsControl" ItemsSource="{Binding SynopticItems}" AllowDrop="True" Drop="EditorCanvas_Drop">
            <ItemsControl.Resources>
                <!-- Template para renderizar um Motor -->
                <DataTemplate DataType="{x:Type models:SynopticMotor}">
                    <StackPanel>
                        <Ellipse Fill="CornflowerBlue" Width="80" Height="80"/>
                        <TextBlock Text="{Binding Label}" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
                <!-- Template para renderizar um Sensor -->
                <DataTemplate DataType="{x:Type models:SynopticSensor}">
                    <Border Background="#FF4A4A4A" Padding="10" CornerRadius="3">
                        <StackPanel>
                            <TextBlock Text="{Binding Label}" Foreground="White"/>
                            <TextBlock Text="{Binding Value}" Foreground="LawnGreen" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.Resources>
            
            <!-- Define o painel onde os itens serão desenhados -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="#FF1E1E1E"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <!-- Define como a posição de cada item é controlada -->
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="EditorItem_PreviewMouseLeftButtonDown"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
</UserControl>