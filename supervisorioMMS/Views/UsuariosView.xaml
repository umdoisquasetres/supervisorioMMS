<UserControl x:Class="supervisorioMMS.Views.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gerenciamento de Usuários" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- User List -->
            <DataGrid Grid.Column="0" x:Name="UsersGrid" Style="{StaticResource SupervisorioDataGridStyle}" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Nível de Acesso" Binding="{Binding AccessLevel}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Add/Edit User Form -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox x:Name="FormGroupBox" Header="Adicionar Usuário" Foreground="White" FontSize="16" FontWeight="Bold" BorderBrush="Gray">
                    <StackPanel Margin="10">
                        <TextBlock Text="Nome:" Foreground="White"/>
                        <TextBox x:Name="NameTextBox" Margin="0,5,0,10" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>

                        <TextBlock Text="Senha:" Foreground="White"/>
                        <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>

                        <TextBlock Text="Nível de Acesso:" Foreground="White"/>
                        <ComboBox x:Name="AccessLevelComboBox" Margin="0,5,0,10">
                            <ComboBoxItem>Operador</ComboBoxItem>
                            <ComboBoxItem>Administrador</ComboBoxItem>
                        </ComboBox>

                        <Button x:Name="AddUserButton" Content="Adicionar" Margin="0,10,0,0" Padding="10,5" Background="#FF007ACC" Foreground="White" Click="AddOrSaveButton_Click"/>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="EditUserButton" Content="Editar Selecionado" Margin="5" Padding="10,5" Background="#FF5A5A5A" Foreground="White" Click="EditButton_Click"/>
                    <Button x:Name="RemoveUserButton" Content="Remover Selecionado" Margin="5" Padding="10,5" Background="#FFDC3545" Foreground="White" Click="RemoveButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>