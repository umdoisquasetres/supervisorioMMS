<UserControl x:Class="supervisorioMMS.Views.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gerenciamento de Usuários" Style="{StaticResource {x:Type TextBlock}}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- User List -->
            <DataGrid Grid.Column="0" x:Name="UsersGrid" Style="{StaticResource SupervisorioDataGridStyle}" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Nível de Acesso" Binding="{Binding AccessLevel}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Add/Edit User Form -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox x:Name="FormGroupBox" Header="Adicionar Usuário" Style="{StaticResource {x:Type GroupBox}}">
                    <StackPanel Margin="10">
                        <TextBlock Text="Nome:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <TextBox x:Name="NameTextBox" Margin="0,5,0,10" Style="{StaticResource {x:Type TextBox}}"/>

                        <TextBlock Text="Senha:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Style="{StaticResource {x:Type PasswordBox}}"/>

                        <TextBlock Text="Nível de Acesso:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <ComboBox x:Name="AccessLevelComboBox" Margin="0,5,0,10" Style="{StaticResource {x:Type ComboBox}}">
                            <ComboBoxItem>Operador</ComboBoxItem>
                            <ComboBoxItem>Administrador</ComboBoxItem>
                        </ComboBox>

                        <Button x:Name="AddUserButton" Content="Adicionar" Margin="0,10,0,0" Click="AddOrSaveButton_Click" Style="{StaticResource {x:Type Button}}"/>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="EditUserButton" Content="Editar Selecionado" Margin="5" Click="EditButton_Click" Style="{StaticResource {x:Type Button}}" Background="{StaticResource SecondaryBackgroundColor}"/>
                    <Button x:Name="RemoveUserButton" Content="Remover Selecionado" Margin="5" Click="RemoveButton_Click" Style="{StaticResource {x:Type Button}}" Background="{StaticResource ErrorColor}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>