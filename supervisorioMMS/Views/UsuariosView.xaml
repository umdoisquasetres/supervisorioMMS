<UserControl x:Class="supervisorioMMS.Views.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:supervisorioMMS.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:UsuariosViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gerenciamento de Usuários" Style="{StaticResource {x:Type TextBlock}}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- User List -->
            <DataGrid Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" Style="{StaticResource SupervisorioDataGridStyle}" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Nível de Acesso" Binding="{Binding AccessLevel}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Add/Edit User Form -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox Header="{Binding FormTitle}" Style="{StaticResource {x:Type GroupBox}}">
                    <StackPanel Margin="10">
                        <TextBlock Text="Nome:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Style="{StaticResource {x:Type TextBox}}"/>

                        <TextBlock Text="Senha:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Style="{StaticResource {x:Type PasswordBox}}"/>
                        <!-- PasswordBox não suporta binding direto por segurança. Acessaremos no ViewModel de outra forma se necessário, mas para este escopo, a lógica atual é suficiente. -->

                        <TextBlock Text="Nível de Acesso:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <ComboBox ItemsSource="{Binding AccessLevels}" SelectedItem="{Binding AccessLevel}" Margin="0,5,0,10" Style="{StaticResource {x:Type ComboBox}}"/>

                        <Button Content="{Binding AddButtonContent}" Command="{Binding AddOrSaveCommand}" Margin="0,10,0,0" Style="{StaticResource {x:Type Button}}"/>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Editar Selecionado" Command="{Binding EditCommand}" Margin="5" Style="{StaticResource {x:Type Button}}" Background="{StaticResource SecondaryBackgroundColor}"/>
                    <Button Content="Remover Selecionado" Command="{Binding RemoveCommand}" Margin="5" Style="{StaticResource {x:Type Button}}" Background="{StaticResource ErrorColor}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>