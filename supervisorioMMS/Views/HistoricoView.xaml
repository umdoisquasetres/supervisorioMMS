<UserControl x:Class="supervisorioMMS.Views.HistoricoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Histórico de Dados" Style="{StaticResource {x:Type TextBlock}}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="De:" Style="{StaticResource {x:Type TextBlock}}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="StartDatePicker" Width="150" Style="{StaticResource {x:Type DatePicker}}"/>
            <TextBlock Text="Até:" Style="{StaticResource {x:Type TextBlock}}" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <DatePicker x:Name="EndDatePicker" Width="150" Style="{StaticResource {x:Type DatePicker}}"/>
            <Button Content="Buscar" Width="100" Margin="10,0,0,0" Style="{StaticResource {x:Type Button}}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="HistoryGrid" Style="{StaticResource SupervisorioDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data/Hora" Binding="{Binding Timestamp}" Width="*"/>
                <DataGridTextColumn Header="Evento/Tag" Binding="{Binding Tag}" Width="2*"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Value}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>