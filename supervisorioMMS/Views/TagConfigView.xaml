<UserControl x:Class="supervisorioMMS.Views.TagConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:supervisorioMMS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configuração de Tags Modbus" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" x:Name="TagsGrid" ItemsSource="{Binding Tags}" Style="{StaticResource SupervisorioDataGridStyle}" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome da Tag" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Endereço" Binding="{Binding Address}" Width="*"/>
                    <DataGridTextColumn Header="Tipo de Dado" Binding="{Binding DataType}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox x:Name="FormGroupBox" Header="Adicionar Tag" Foreground="White" FontSize="16" FontWeight="Bold" BorderBrush="Gray">
                    <StackPanel Margin="10">
                        <TextBlock Text="Nome:" Foreground="White"/>
                        <TextBox x:Name="NameTextBox" Margin="0,5,0,10" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>

                        <TextBlock Text="Endereço Modbus:" Foreground="White"/>
                        <TextBox x:Name="AddressTextBox" Margin="0,5,0,10" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>

                        <TextBlock Text="Tipo de Dado:" Foreground="White"/>
                        <ComboBox x:Name="DataTypeComboBox" Margin="0,5,0,10"/>

                        <Button x:Name="AddButton" Content="Adicionar" Margin="0,10,0,0" Padding="10,5" Background="#FF007ACC" Foreground="White" Click="AddOrSaveButton_Click"/>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="EditButton" Content="Editar Selecionada" Margin="5" Padding="10,5" Background="#FF5A5A5A" Foreground="White" Click="EditButton_Click"/>
                    <Button x:Name="RemoveButton" Content="Remover Selecionada" Margin="5" Padding="10,5" Background="#FFDC3545" Foreground="White" Click="RemoveButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>