<UserControl x:Class="supervisorioMMS.Views.TagConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:supervisorioMMS.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:TagConfigViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configuração de Tags Modbus" Style="{StaticResource {x:Type TextBlock}}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Tags}" SelectedItem="{Binding SelectedTag}" Style="{StaticResource SupervisorioDataGridStyle}" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome da Tag" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Endereço" Binding="{Binding Address}" Width="*"/>
                    <DataGridTextColumn Header="Tipo de Dado" Binding="{Binding DataType}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox Header="{Binding FormTitle}" Style="{StaticResource {x:Type GroupBox}}">
                    <StackPanel Margin="10">
                        <TextBlock Text="Nome:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Style="{StaticResource {x:Type TextBox}}"/>

                        <TextBlock Text="Endereço Modbus:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Style="{StaticResource {x:Type TextBox}}"/>

                        <TextBlock Text="Tipo de Dado:" Style="{StaticResource {x:Type TextBlock}}"/>
                        <ComboBox ItemsSource="{Binding DataTypes}" SelectedItem="{Binding DataType}" Margin="0,5,0,10" Style="{StaticResource {x:Type ComboBox}}"/>

                        <Button Content="{Binding AddButtonContent}" Command="{Binding AddOrSaveCommand}" Margin="0,10,0,0" Style="{StaticResource {x:Type Button}}"/>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Editar Selecionada" Command="{Binding EditCommand}" Margin="5" Style="{StaticResource {x:Type Button}}" Background="{StaticResource SecondaryBackgroundColor}"/>
                    <Button Content="Remover Selecionada" Command="{Binding RemoveCommand}" Margin="5" Style="{StaticResource {x:Type Button}}" Background="{StaticResource ErrorColor}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>