<UserControl x:Class="supervisorioMMS.Views.ConfiguracoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:supervisorioMMS.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:ConfiguracoesViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configurações de Conexão" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,10,0">
            <StackPanel IsEnabled="{Binding IsSettingsEnabled}">
                <GroupBox Header="1. Selecione o Tipo de Conexão">
                    <ComboBox ItemsSource="{Binding ConnectionTypes}" SelectedItem="{Binding SelectedConnectionType}" Padding="8" FontWeight="Bold"/>
                </GroupBox>

                <GroupBox Header="2. Parâmetros Modbus RTU (Serial)" Margin="0,15,0,0" Visibility="{Binding IsRtuSelected, Converter={StaticResource BoolToVis}}">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Porta COM:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ComPorts}" SelectedItem="{Binding SelectedComPort}" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Baud Rate:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Data Bits:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding DataBitsOptions}" SelectedItem="{Binding SelectedDataBits}" Margin="0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Parity:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding ParityOptions}" SelectedItem="{Binding SelectedParity}" Margin="0,5"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Stop Bits:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding StopBitsOptions}" SelectedItem="{Binding SelectedStopBits}" Margin="0,5"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="2. Parâmetros Modbus TCP/IP" Margin="0,15,0,0" Visibility="{Binding IsTcpSelected, Converter={StaticResource BoolToVis}}">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Endereço IP:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding IpAddress}" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Porta:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Port}" Margin="0,5"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Fill="{Binding StatusIndicatorFill}" Width="15" Height="15" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusText}" FontWeight="Bold" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>

            <Button Grid.Column="1" Content="{Binding ConnectButtonContent}" Command="{Binding ConnectCommand}" Width="150" Height="40" FontSize="16">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                <Setter Property="Background" Value="{StaticResource ErrorColor}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>
