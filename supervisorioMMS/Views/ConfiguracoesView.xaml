<UserControl x:Class="supervisorioMMS.Views.ConfiguracoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configurações de Conexão" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,10,0">
            <StackPanel>
                <GroupBox Header="1. Selecione o Tipo de Conexão">
                    <ComboBox x:Name="ConnectionTypeComboBox" SelectionChanged="ConnectionTypeComboBox_SelectionChanged" Padding="8">
                        <ComboBoxItem Content="Modbus RTU (Serial)"/>
                        <ComboBoxItem Content="Modbus TCP/IP"/>
                    </ComboBox>
                </GroupBox>

                <GroupBox x:Name="RtuSettingsGroupBox" Header="2. Parâmetros Modbus RTU (Serial)" Margin="0,15,0,0">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Porta COM:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox x:Name="ComPortComboBox" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Baud Rate:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox x:Name="BaudRateComboBox" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Data Bits:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox x:Name="DataBitsComboBox" Grid.Row="2" Grid.Column="1" Margin="0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Parity:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox x:Name="ParityComboBox" Grid.Row="3" Grid.Column="1" Margin="0,5"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Stop Bits:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <ComboBox x:Name="StopBitsComboBox" Grid.Row="4" Grid.Column="1" Margin="0,5"/>
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="TcpSettingsGroupBox" Header="2. Parâmetros Modbus TCP/IP" Margin="0,15,0,0" Visibility="Collapsed">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Endereço IP:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <TextBox x:Name="IpAddressTextBox" Grid.Row="0" Grid.Column="1" Text="127.0.0.1" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Porta:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <TextBox x:Name="PortTextBox" Grid.Row="1" Grid.Column="1" Text="502" Margin="0,5"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse x:Name="StatusIndicator" Width="15" Height="15" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" FontWeight="Bold" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>

            <Button x:Name="ConnectButton" Grid.Column="1" Width="150" Height="40" Click="ConnectButton_Click" FontSize="16"/>
        </Grid>
    </Grid>
</UserControl>
