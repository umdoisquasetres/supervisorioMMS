<UserControl x:Class="supervisorioMMS.Views.ConfiguracoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configurações do Sistema" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <!-- Connection Settings -->
            <GroupBox Header="Conexão com CLP" Foreground="White" FontSize="16" FontWeight="Bold" BorderBrush="Gray">
                <StackPanel Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Endereço IP:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBox x:Name="IpAddressTextBox" Grid.Column="1" Text="127.0.0.1" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>
                    </Grid>
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Porta:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="1" Text="502" Background="#FF3E3E3E" Foreground="White" BorderBrush="Gray" Padding="5"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <!-- General Settings -->
            <GroupBox Header="Geral" Foreground="White" FontSize="16" FontWeight="Bold" BorderBrush="Gray" Margin="0,20,0,0">
                <StackPanel Margin="10">
                    <CheckBox Content="Habilitar notificações por email" IsChecked="True" Foreground="White"/>
                    <CheckBox Content="Salvar log de eventos em arquivo" IsChecked="True" Foreground="White" Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>

            <Button Content="Salvar" Width="120" HorizontalAlignment="Right" Margin="0,20,0,0" Padding="10,5" Background="#FF007ACC" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>