<Window x:Class="supervisorioMMS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:supervisorioMMS"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="SCADA Supervisório" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">
    <Window.Resources>
        <Style TargetType="Button" x:Key="NavButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF3E3E3E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="#FF007ACC" Padding="15">
            <TextBlock Text="Sistema Supervisório SCADA - MMS" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <!-- Footer -->
        <Border DockPanel.Dock="Bottom" Background="#FF007ACC" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Usuário: Admin" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Grid.Column="1" x:Name="StatusText" Text="Pronto" Foreground="LawnGreen" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='dd/MM/yyyy HH:mm:ss'}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0"/>
            </Grid>
        </Border>

        <!-- Navigation -->
        <Border DockPanel.Dock="Left" Background="#FF2D2D2D" Width="200">
            <StackPanel>
                <Button x:Name="NavPrincipal" Content="Tela Principal" Style="{StaticResource NavButton}" Click="NavPrincipal_Click"/>
                <Button x:Name="NavAlarmes" Content="Alarmes" Style="{StaticResource NavButton}" Click="NavAlarmes_Click"/>
                <Button x:Name="NavHistorico" Content="Histórico" Style="{StaticResource NavButton}" Click="NavHistorico_Click"/>
                <Button x:Name="NavGraficos" Content="Gráficos" Style="{StaticResource NavButton}" Click="NavGraficos_Click"/>
                <Button x:Name="NavConfiguracoes" Content="Configurações" Style="{StaticResource NavButton}" Click="NavConfiguracoes_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ContentControl x:Name="MainContent" Margin="20"/>
    </DockPanel>
</Window>